# Task 2 实践成果

2026-09-15 收录两项实践：P1 场景评估（4 个问题）与 D1 的 d1_1～d1_4 真实模型运行。I1 数据底座评审按用户选择本次不写，已有 I1 学习心得保留。

## P1：医院项目部署前的版本核对助手

实际需求：帮助交付核对医院当前版本、目标版本、升级脚本和回归测试范围，给出资料依据；实际部署由人操作。

| 典型问题 | 用户提供的数据来源 | 当前需要解决的问题 |
| --- | --- | --- |
| 医院现在是什么版本？ | SM | 有权限限制，记录也可能与实际部署不一致 |
| 这次准备升级到哪个版本？ | RM 版本、打包记录，以及通知交付的信息 | 要关联具体医院的本次升级安排，不能直接取最新版本 |
| 需要执行哪些增量脚本？ | 对应项目中的增量脚本 | 要结合起止版本和执行记录判断 |
| 哪些地方需要重新测试，有什么风险？ | 当前由用户在发版时说明修复、需求和测试范围 | 期望 AI 根据 commit 和代码改动辅助总结；需求背景、验收标准和风险推断仍需核实 |

根据这些事实，当前应先理清数据来源、权限和版本关联。已确认完整、准确且能被 Agent 直接获取的数据为 **0/4**，这不表示数据不存在，只表示尚未核实。没有实际访问 SM、RM 或公司仓库。

完整 P1 场景评估已归档在任务目录的 p1-assessment.md。课程要求 10 个典型问题，本次按实际讨论收录 4 个，其余 6 个和完整 Checklist 尚未补齐。

## D1：四个示例的实际运行结果

**4 项通过、0 项失败、0 项跳过。** 2026-09-15 11:31（Asia/Shanghai），在 macOS、Python 3.11.15 环境运行，使用项目已配置的 `deepseek-v4-pro`，共 5 次模型请求。以下为本次实际返回内容，不是预写示例答案。

### 运行方式与验证范围

- 原样调用官方 d1_1～d1_4 的 `run_demo`；仅模型初始化改用项目 DeepSeek 配置，没有执行官方 `main` 的 SiliconFlow 配置入口。
- d1_2 使用课程预先写好的 4 条消息验证上下文传递，不是现场连续发起两轮请求。
- d1_3 记录流式分片和首个正文分片时间；报告中展示合并后的正文。
- d1_4 调用真实模型，工具查询课程模拟知识库。保留官方兼容模式，工具结果以 user 消息回传，未验证标准 ToolMessage 协议路径。
- 关闭思考模式，单次请求输出上限 2048 tokens、超时 90 秒、自动重试 0 次；工具循环最多 5 轮。

| 示例 | 结果 | 总耗时 | 观察到的行为 |
| --- | --- | --- | --- |
| d1_1_base（基础调用） | PASS | 2.354 秒 | 收到非空的 RAG 解释 |
| d1_2_multi_turn（多轮消息） | PASS | 2.199 秒 | 携带 4 条历史消息，回答后续对比问题 |
| d1_3_streaming（流式输出） | PASS | 1.914 秒 | 61 个总分片、58 个正文分片，首个正文分片 0.624 秒 |
| d1_4_tool_use_mock（模拟知识库工具调用） | PASS | 4.284 秒 | 2 次模型请求、2 次工具执行，最后返回非空回答 |

[原始结构化报告（TXT 导出）](results/20260915-113114-d1.txt)（与 JSON 报告内容相同，含依赖版本、课程提交、源码 SHA-256、请求事件、工具参数及完整输出）。

### d1_1_base：基础调用

```text
RAG（检索增强生成）是一种结合信息检索与语言模型生成的技术，先从外部知识库检索相关内容，再让模型基于这些内容生成更准确的回答。
```

### d1_2_multi_turn：多轮消息

```text
主要区别：

- **RAG**：从知识库里检索最相关的片段，只把选中的内容放进 Prompt，省 token、更精准。
- **直接塞全文**：把整份文档放进 Prompt，简单直接，但文档长会超限、成本高、干扰也多。

RAG 适合文档量大或频繁更新的场景。
```

### d1_3_streaming：流式输出

```text
向量数据库是一种专门用于存储、索引和查询高维向量数据的数据库系统。它通过计算向量之间的相似度（如余弦相似度或欧氏距离）来快速检索最相关的数据。它广泛应用于大语言模型、图像检索、推荐系统等需要语义匹配的场景。
```

### d1_4_tool_use_mock：模拟知识库工具调用

```text
>>> 用户提问：seekdb 支持哪些检索方式？混合检索是怎么实现的？
>>> 模型决定调用工具：query_knowledge_base
>>> 工具参数：{'query': 'seekdb 支持的检索方式有哪些'}

>>> 知识库检索结果：
seekdb 支持关键词、语义向量和混合检索。

seekdb 支持嵌入模式和服务器模式。

>>> 模型决定调用工具：query_knowledge_base
>>> 工具参数：{'query': 'seekdb 混合检索的实现原理'}

>>> 知识库检索结果：
seekdb 支持关键词、语义向量和混合检索。

>>> 模型最终回答：
seekdb 支持关键词检索、语义向量检索和混合检索，并且提供嵌入模式和服务器模式两种运行模式。

关于混合检索的实现原理，知识库中暂未检索到更详细的说明，无法给出具体的技术实现细节。如果你需要，我可以帮你进一步查询或建议你查阅官方技术文档确认。
```

### 结果解读

这次验证了基础调用、历史消息传递、真实流式输出，以及“模型请求工具 → 本地执行 → 结果回传 → 最终回答”的完整调用链路。d1_4 的模拟知识库缺少混合检索实现细节，模型明确说明资料不足，没有把不存在的说明当成检索结果。

首次运行在客户端初始化时发现缺少 SOCKS 代理依赖，尚未发出模型请求；安装 `socksio` 后再次运行，上述四项通过。首次失败报告也保存在任务 results 目录中，未覆盖。

## 当前完成边界

- P1：收录 4 个真实问题及数据可得性评估，尚未完成课程要求的 10 个问题和完整 Checklist。
- D1：d1_1～d1_4 已验证；d1_5 未运行，d1_6 附加题未运行。
- I1：用户选择本次不写数据底座评审，已有学习心得保留。
- 以上为实践执行记录，不代表全部课程完成或表单已提交。
